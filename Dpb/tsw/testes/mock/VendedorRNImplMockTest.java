package testes.mock;

import static org.junit.Assert.assertEquals;

import java.util.Arrays;
import java.util.List;

import org.junit.Test;
import org.mockito.Mockito;

import model.classesbasicas.Vendedor;

public class VendedorRNImplMockTest {

	@Test
	public void usandoMockito() {
		
//		IVendedorMockTest iVendedorMockTest = Mockito.mock(IVendedorMockTest.class);
//		List<Vendedor> allVend = Arrays.asList("Learn Spring MVC", "Learn Spring", "Learn to Dance");
//		when(IVendedorMockTest.retrieveTodos("Ranga")).thenReturn(allVend);
//		VendedorRNMockTest vendedorRNMockTest = new VendedorRNMockTest(iVendedorMockTest);
//		List<Vendedor> alls = vendedorRNMockTest.allVend(vendedor);
//		assertEquals(2, alls.size());
		
	}
}
